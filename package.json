{
  "name": "agent-cli-contract-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite site",
    "generate:recording": "cargo build && node scripts/generate-terminal-recording.mjs",
    "build:site": "npm run generate:recording && vite build site --config vite.config.ts --outDir ../dist/site --emptyOutDir",
    "build:cli": "cargo build --release",
    "build": "npm run build:site && npm run build:cli",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/ensure-rust-toolchain.mjs && cargo test && cargo build && npm run typecheck && npm run build:site && playwright test",
    "test:claims": "npm test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "@types/node": "22.15.3",
    "yaml": "2.9.0"
  },
  "overrides": {
    "playwright-core": "1.58.2"
  }
}
