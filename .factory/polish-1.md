# Polish round 1 — PASS

Reviewed and repaired against `.factory/review-1.md` and the earlier verification reports. No earlier polish report existed. Production URL: <https://agent-cli-contract.sociobot.in>.

| Finding | Change made | Evidence |
| --- | --- | --- |
| F-1-1 | Replaced the timer-driven fake run with “Replay recorded sample run,” a copyable `agent-contract demo` command, an accessible transcript, and a self-hosted SVG generated by running the real binary. The CLI now prints every fixture name, mode, exit, pass count, and report path. | `@claim:recorded-demo matches a fresh bundled CLI demo report`; `site/public/terminal-recording.svg`; `.factory/evidence/live-polish-final.json` → `honest-recording`, `recording-match`; `.factory/evidence/live-polish-demo/screenshot-desktop.png`; live `/?demo=1`. |
| F-1-2 | Store scroll coordinates in each history entry. On Back/Forward, render, wait for layout, restore without smooth-scroll drift, focus the h1 with `preventScroll`, and announce it. | `Back restores landing scroll while focusing its heading` passed 10 repeated local runs; `.factory/evidence/live-polish-history.json` records 1200 → 1200 and focused h1; `.factory/evidence/live-polish-back-restored.png`; live `/` ↔ `/demo`. |
| F-1-3 | Kept the demo banner sticky at 390 px, compacted its two 44 px controls, and retained zero page overflow. | `390px routes do not create horizontal overflow and utility targets are touch sized`; `.factory/evidence/live-polish-final.json` → `mobile-sticky`; `.factory/evidence/live-polish-mobile-sticky.png`; live `/?demo=1`. |
| F-1-4 | Changed “declared route” and “Declare the route” to “declared command” and “Declare the command.” | `.factory/copy-audit.md`; `.factory/evidence/live-polish-home/screenshot-desktop.png`; live `/`. |
| F-1-5 | Rewrote the audience sentence to name human-readable output and changed the privacy heading to “Run only commands you declare.” | `.factory/copy-audit.md`; `first-screen-copy` in `.factory/evidence/live-polish-final.json`; live `/`. |
| F-1-6 | Renamed the action to “Leave demo and view install steps” while preserving demo-key deletion and real-key isolation. | `@claim:demo-sandbox keeps demo state separate and reset removes it`; `.factory/evidence/live-polish-final.json` → `leave-demo`; live `/?demo=1`. |
| F-1-7 | Added `local-execution` to the claims manifest and a fixture test that verifies network-denied local execution plus local snapshot and report files. | `@claim:local-execution writes local outputs with fixture networking denied`; `.factory/polish-1-final-claim-sweep.txt`. |
| F-1-8 | Added `default-timeout` to the claims manifest and measured the implicit 10,000 ms limit against a 20-second fixture. | `@claim:default-timeout applies the documented 10 second limit`; `.factory/polish-1-final-claim-sweep.txt`. |
| F-1-9 | Removed the untestable forward compatibility promise. Project status now links only to released behavior. | `README.md` Project status; clean-copy search and `.factory/copy-audit.md`. |
| F-1-10 | Replaced the provenance promise with the finite statement that the runner executes only the literal command declared in the contract. | `@claim:direct-execution` and `@claim:declared-commands`; `.factory/polish-1-final-claim-sweep.txt`. |
| F-1-11 | Extended `network-opt-in` with a local HTTP server: undeclared curl and Node fetch stay blocked, while `allow_network: true` reaches the server and snapshots its response. | `@claim:network-opt-in blocks direct and runtime network access by default`; `.factory/polish-1-final-claim-sweep.txt`. |

## Cumulative structure and release evidence

- One-click demo: `/?demo=1`; `/demo` remains a canonical route. Reset and exit remove only `demo:` keys.
- Route tests cover unique titles, descriptions, canonical and social metadata, h1/main structure, focus, and all legal/navigation routes.
- Unknown URLs return HTTP 404 through built `404.html`; evidence: `.factory/evidence/live-polish-404-headers.txt` and `.factory/evidence/live-polish-404.png`.
- All 25 claim commands passed separately from clean clone `a09b67c191c4b18f4114ee2bf816966c63ece7e2`; full output: `.factory/polish-1-final-claim-sweep.txt`.
- The same clean clone passed all 32 Playwright/unit/integration/accessibility/privacy/offline tests, `npm run build`, `cargo fmt --check`, strict Clippy, and `cargo package`; output: `.factory/polish-1-final-clean-suite.txt`.
- RustSec and npm audits found zero vulnerabilities; output: `.factory/polish-1-security.txt`.
- Live Lighthouse: performance 99, accessibility 100, best practices 100, SEO 100; LCP 1.97 s, CLS 0, TBT 21 ms. Evidence: `.factory/evidence/lighthouse-polish-live.json`.
- Both live `verify-url.sh` runs report no console errors and valid title/lang/h1/main/alt coverage: `.factory/evidence/live-polish-home/verify.json` and `.factory/evidence/live-polish-demo/verify.json`.

No finding remains open.
