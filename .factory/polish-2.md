# Polish round 2 — repaired candidate

Repaired from release candidate `3ec47269c9a627ade759d7646685acbdf065e033` using every finding in `review-1.md` and `review-2.md`. Repair commit: `1d1f36658153632ef1e93abb135a33cc9b840c9b`.

| Finding | Change made | Evidence |
| --- | --- | --- |
| F-1-1 | Browser action explicitly replays a self-hosted recording generated by `agent-contract demo`; the runnable command and transcript remain beside it. | `@claim:recorded-demo`; `site/public/terminal-recording.svg`; `/?demo=1`. |
| F-1-2 | History state records coordinates and restores scroll before focusing the route heading. | `Back restores landing scroll while focusing its heading` Playwright test. |
| F-1-3 | Demo status remains a compact sticky banner with both 44 px actions at 390 px. | `390px routes do not create horizontal overflow and utility targets are touch sized` Playwright test. |
| F-1-4 | Landing terminology uses **command** for the declared executable and arguments. | `.factory/copy-audit.md`; landing source and browser test suite. |
| F-1-5 | Audience and privacy copy identify human-readable output and declared commands in plain words. | `.factory/copy-audit.md`; `desktop first screen keeps the primary action and all facts above a 768px fold`. |
| F-1-6 | Demo exit says “Leave demo and view install steps” and clears only the demo namespace. | `@claim:demo-sandbox`. |
| F-1-7 | “Runs locally” is listed as `local-execution` and tested with fixture networking denied. | `@claim:local-execution`. |
| F-1-8 | The 10-second default timeout is listed and measured against a 20-second fixture. | `@claim:default-timeout`. |
| F-1-9 | Removed the untestable future compatibility promise. | README copy audit and clean-clone claim sweep. |
| F-1-10 | The safety wording is limited to the tested declared-command behavior. | `@claim:declared-commands`; `@claim:direct-execution`. |
| F-1-11 | Network opt-in is tested both blocked and allowed against a local HTTP server. | `@claim:network-opt-in`. |
| F-2-1 | On screens up to 520 px, the sample report is ordered before the fixture sheet and the demo intro is compacted. The report heading and first fixture row now appear in the initial 390 × 844 viewport. | `mobile demo shows a sample result in the initial viewport and links resolve`; `.factory/evidence/polish-2-mobile-demo.png`; local `/?demo=1`. |
| F-2-2 | Removed the internal “factory” release-process sentence from README. | `.factory/copy-audit.md`; README. |
| F-2-3 | Added the documented `nondeterministic-field-allowlist` claim and a changing `$.meta.duration_ms` fixture that passes only when that path is allowlisted. | `@claim:nondeterministic-field-allowlist`. |
| F-2-4 | Added the `schema-output` claim and parsed the real command output for every documented root and fixture field. | `@claim:schema-output`. |
| F-2-5 | Added the `contract-format-version` claim, accepting version 1 and rejecting version 2 with the documented guidance. | `@claim:contract-format-version`. |

## Verification

- Fresh clone: `/tmp/agent-cli-contract-polish-2-FI9Nk8/repo` at `1d1f36658153632ef1e93abb135a33cc9b840c9b`.
- Every one of the 28 commands in `.factory/claims.json` passed separately from that clone, including all three new claim commands.
- `npm test` passed from the same clone: 3 Rust unit tests and 35 Playwright tests, including route metadata, real 404, keyboard skip link, offline reload, privacy isolation, no third-party demo requests, Axe serious/critical scan, mobile sizing, and the report-in-first-viewport assertion.
- `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo package`, and `npm audit --omit=dev` passed from that clone. The package was verified by Cargo; npm reported zero vulnerabilities.
- Production site build: JS 18.42 kB (5.87 kB gzip), CSS 14.97 kB (4.25 kB gzip), hero artwork under the 300 kB budget.
- Screenshots: `.factory/evidence/polish-2-home-desktop.png` and `.factory/evidence/polish-2-mobile-demo.png` from the built site at `http://127.0.0.1:4174/`; `.factory/evidence/polish-2-live-mobile-demo.png` from the deployed custom domain.
- Deployment: `/opt/fleet/lib/deploy-static.sh agent-cli-contract dist/site` completed successfully as Azure deployment `2df3f06e-d4be-4836-a5fa-51bd0edb050c`. Cold live `https://agent-cli-contract.sociobot.in/?demo=1` loaded the Demo title and heading, showed the PASS report at 676.7 px and its first result row at 834.5 px in a 390 × 844 viewport, kept the banner sticky after scroll, and passed blocked-change then reset. Normal live routes `/`, `/?demo=1`, `/privacy`, and `/terms` produced no console errors. `/missing-route` returned HTTP 404 and the designed 404 title.

The repair commit is pushed to `origin/main` and the static deployment was cold-checked successfully.
